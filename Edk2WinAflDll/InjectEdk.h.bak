#ifndef _INJECT_EDK_H_
#define _INJECT_EDK_H_

#define addrInject 0x10000000
inline void callAflProxyDll(int point) {
 /* __asm {
    push point
    mov eax, addr
    call[eax]
    add  esp, 4
  }
  */
  typedef void func(int);
  func* f = (func*)addrInject;
  f(point);
}
#endif